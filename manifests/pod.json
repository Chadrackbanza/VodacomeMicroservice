{
  "kind": "Pod",
  "metadata": { "name": "test-pod" },
  "spec": {
    "containers": [
      {
        "name": "app-container",
        "env": [
          { "name": "DB_PASSWORD", "value": "my-secret-password" }
        ]
      }
    ]
  }
}
